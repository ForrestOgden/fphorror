# Cached release assets

Run `npm run assets` from the project root to cache the Poly Haven environment assets and CC0 recorded audio used by NULL FLOOR.

The game loads this directory first. If a file is absent, it falls back to its remote development source; essential audio/material systems also retain procedural fallbacks.
